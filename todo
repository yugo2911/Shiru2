playerpage
    expand video to height (no black bars preference)
    selected subs should have a hint have no idea  
    reposition video from top left top bottom left
    skip torrents selection menu (fully auto select)(this belongs in more menu)
    fix signs from jp subs not loading correctly so fonts are not being loaded correctly. 
    as for example in mpv they are way diffrent lookin, and in shiru they look  arial or someshit its prob fails to load and defaults to what ever colors load correctly hm?

schedulepage
    miniplayer prevents hover images appearing at all
    *new* mark, or and user *status* mars
    move all styles to css stroe as vars
    cached at runtime or i mean shiru already does this onload hm? well we just need names& air time fetched quickly
    relations
    

jimaku
    use newly added css/vars


details page
    remove artwork and horizontally place episodes
    vertical japanese titiles
    move favorite button to editor page
    next episode timer for airing shows.

settings
    search settings via input
    search subtitiles via input
    selecting subtitiles freezes app


small cards
    show on hover airing status
    remove year
    choose/randomize cover artwork from other providers kitsu etc.

